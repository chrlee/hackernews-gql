{
  "name": "hackernews_gql",
  "version": "0.1.0",
  "description": "Hackernews API converted to GraphQL",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bun build --target bun src/server.ts --minify --compile",
    "start": "bunx public/server.js",
    "dev": "bunx --hot src/server.ts",
    "clean": "rm -rf node_modules && rm -rf build && rm bun.lockb",
    "lint": "prettier --plugin-search-dir . --check . && eslint .",
    "format": "prettier --plugin-search-dir . --write ."
  },
  "author": {
    "name": "chrlee",
    "email": "christianjslee@gmail.com"
  },
  "license": "MIT",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.7.5",
    "@typescript-eslint/parser": "^6.7.5",
    "bun": "^1.0.6",
    "bun-types": "^1.0.6",
    "eslint": "^8.51.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-prettier": "^5.0.1",
    "prettier": "^3.0.3",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "dataloader": "^2.2.2",
    "firebase": "10.5.0",
    "graphql": "^16.8.1",
    "graphql-yoga": "4.0.5",
    "nexus": "^1.3.0",
    "vercel": "latest"
  }
}
